# 1. 忽略通义千问API配置文件（根据你的实际文件名修改，比如.env、config.ini）
.env
config.ini
api_config.py
*api*.py
*config*.ini
# 2. 忽略exe大文件（如果后续想保留exe在仓库，可删除这两行；不想保留则保留）
# *.exe
# tools/*.exe
# 3. 忽略其他常见敏感/临时文件
*.log
*.tmp
.idea/
.vscode/
__pycache__/
*.exe filter=lfs diff=lfs merge=lfs -text
tools/nuclei.exe filter=lfs diff=lfs merge=lfs -text
